"use client";

import { useState, useEffect, useRef, useCallback } from "react";
import { TrafficSignalDashboard } from "@/components/signals/TrafficSignalDashboard";

export default function TrafficSignalsPage() {
  return <TrafficSignalDashboard />;
}
